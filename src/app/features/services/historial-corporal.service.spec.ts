/* tslint:disable:no-unused-variable */

import { TestBed, inject } from '@angular/core/testing';
import { HistorialCorporalService } from './historial-corporal.service';

describe('Service: HistorialCorporal', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [HistorialCorporalService]
    });
  });

  it('should ...', inject([HistorialCorporalService], (service: HistorialCorporalService) => {
    expect(service).toBeTruthy();
  }));
});
